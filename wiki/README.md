# AI ML Onboarding Wiki

This folder stores generated reference material for the onboarding assistant.

- `prompts/`: exported prompt templates from `prompt_templates.json`

Run `./ml-agent init` or `.\ml-agent.cmd init` to refresh generated wiki files.
